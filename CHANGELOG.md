# Changelog

本文件记录面向用户的应用发布变更，版本号遵循语义化版本规范。

## [0.15.3] - 2026-09-21

### 修复

- 导入备份后的笔记没有同步到云端。原因是备份里的笔记如果曾在云端被彻底删除（含回收站自动清理），服务端还留着 30 天的删除标记，用原 id 重新创建时服务端直接跳过却回报成功，云端仍是删除态，换设备一同步就没了。现在创建接口遇到删除标记会把它复活为正常笔记，并推进版本号让所有设备重新拉取。独立服务器版同样受影响，升级后即修复；已经导入过的笔记，在本机随便改一下再保存即可推上去。

## [0.15.2] - 2026-09-21

### 修复

- 导入备份时分组按名字复用本机已有的分组，不再每次导入都多出一个同名分组。
- 回收站里的搜索框下方多出的一条分隔线去掉（它来自顶部的保留期提示，提示已删除）。

### 变更

- 回收站保留期默认 30 天：进入回收站满 30 天的笔记由每日维护任务自动彻底删除。列表里每条显示「N 天后删除」，详情顶栏的「此笔记在回收站中」改为「此笔记还有 N 天被删除」。要改天数或关闭，设置 `TRASH_RETENTION_DAYS`（`0` 或 `off` 关闭）。
- 回收站详情顶栏只保留「恢复」和「彻底删除」，去掉字数、文档信息、历史版本和「⋯」菜单。
- 新建笔记快捷键改为 `Alt / Option + N`。`Ctrl + Alt` 在 Windows 上等同 AltGr，常被输入法和键盘布局吃掉。

## [0.15.1] - 2026-09-21

### 变更

- 「历史版本」弹窗去掉顶部的规则说明文字，只在读取云端时短暂显示「正在读取云端…」。

## [0.15.0] - 2026-09-20

### 新增

- 正文历史加入云端：服务端保存正文时按与本机相同的规则（间隔满 5 分钟或一次改动较大）把被覆盖的旧正文留一条，每条笔记保留 20 条、30 天，彻底删除时连带清理。「历史版本」弹窗把本机与云端的版本合并显示，同一份正文只列一条并标出来源；换设备也能找回其他设备留存的版本。从历史恢复前，当前正文会同时存入本机与云端。旧服务端没有这个接口时只显示本机部分。数据库新增迁移 `0004_note_history.sql`，Cloudflare 部署需执行 `npx wrangler d1 migrations apply snotes --remote`。
- 回收站顶部提示保留策略：服务端开启了 `TRASH_RETENTION_DAYS` 时说明几天后自动彻底删除，每条笔记显示还剩几天；未开启时说明会一直保留。

### 变更

- 桌面正文顶栏直接显示字数、文档信息、历史版本三个按钮，点字数打开详细统计；「⋯」菜单只保留复制、下载、分享、打印。手机上这三项仍在「⋯」里。正文底栏（「自动保存 · N 字」）整个去掉。
- 侧栏底部不再显示版本号，版本只在设置「关于」页查看；有新版本时提示点仍挂在图标栏「设置」上。
- 新建笔记快捷键改为 `Ctrl / Cmd + Alt + N`：`Ctrl + N` 会被浏览器抢去打开新窗口，网页无法拦截。去掉 `Ctrl / Cmd + Shift + 0–9` 切换分组的快捷键，它们与系统和输入法快捷键冲突。
- 设置页删去几段说明文字（导出包结构、本机数据、退出说明、外观设置说明），Markdown 速查下的图片提示改为「关于图片：电脑端可以直接粘贴，手机上可用顶栏的“插入图片”按钮」。

## [0.14.0] - 2026-09-20

### 变更

- 设置入口搬到左侧图标栏底部，桌面是图标、手机抽屉里带「设置」文字，不再藏在侧栏底角的小齿轮里。设置改为分页大弹窗：**外观**（主题、字号、编辑区宽度）、**数据**（原「导出与导入」）、**快捷键**（全局快捷键、编辑器内快捷键、Markdown 速查，原「格式帮助」）、**关于**（原「版本信息」弹窗：版本、升级步骤、发布链接）。侧栏底部只留同步状态和版本号，点版本号直达「关于」页；有新版本时图标栏「设置」和版本号一起显示蓝点。新增快捷键 `Ctrl / Cmd + ,` 打开设置。系统返回键可以像关闭统计一样关闭设置。
- 正文顶栏最右新增「⋯」菜单，收拢文档信息、历史版本、字数统计、复制 Markdown、下载 .md、分享到其他应用。正文底栏不再放任何按钮：桌面只显示「自动保存 · N 字」，手机上整条隐藏，把空间还给正文。原因是底栏贴着屏幕底边，手机浏览器工具栏、系统手势区和软键盘都压在那一带，经常点不到。
- 手机（宽度 ≤ 720px）顶栏放不下第八个按钮，「删除」让位给「⋯」，从菜单里删或在列表左滑删除；桌面顶栏的删除按钮和 `Ctrl / Cmd + Shift + D` 不变。回收站里的「⋯」只保留信息、历史、字数、复制、下载、打印。
- 历史版本从「文档信息」弹窗里拆出来，成为「⋯」菜单里独立的「历史版本」条目和弹窗；预览、恢复、只读态的行为不变。
- 复制、下载、分享、恢复历史、复制链接这类操作提示改为顶部居中浮出的短消息，3.5 秒自动消失，不再显示在底栏。

### 新增

- 「⋯」菜单里的「打印 / 存为 PDF」，手机上也能打印当前笔记（打印样式沿用 0.12.0）。
- 设置「关于」页尾的「退出登录」：确认后只清除本机保存的访问令牌，本机已同步的笔记保留，界面回到令牌页。

## [0.13.0] - 2026-09-20

### 新增

- 本地正文历史。以前误删一大段、刷新后撤销栈没了就找不回来，现在每次正文落库时，如果距上一条留存满 5 分钟、或者一次改动的可见字符超过 100 个，就把被替换的旧正文自动存一条；每条笔记保留最近 20 条且不超过 30 天，笔记被彻底删除（含清空回收站和其他设备删除后同步过来）时连带清理。文档信息弹窗新增「历史版本」列表，显示时间与字数，可展开预览、一键恢复；恢复前会先把当前正文也存进历史，恢复错了还能再恢复回来。回收站里的笔记只能预览不能恢复。历史只保存在本设备的浏览器里，不参与同步、不进入导出包。
- 本地数据库升级到第 2 版（新增历史表），旧数据原样保留，打开新版本时自动完成。

## [0.12.0] - 2026-09-20

### 新增

- 登录限流（独立服务器版）。同一来源连续 5 次令牌错误后封锁 1 分钟，封锁期满再错则封锁时长翻倍，最长 1 小时；成功一次或安静 15 分钟后清零。封锁期间该来源的全部 `/api/` 请求返回 429 并带 `Retry-After`，`/api/health` 不计入也不受影响。来源按 TCP 对端地址判定，只有来自本机或内网反向代理的 `X-Forwarded-For` 才被采信，公网直连的客户端伪造该头不会换来新的额度。计数保存在进程内存里，重启即清空。网页收到这种 429 时会回到令牌输入页并提示需要等待的时间。Cloudflare 版不在代码里限流，README 的「安全说明」给出了 WAF 速率限制规则的配置建议。
- 打印样式。按 Ctrl/⌘ P 打印或存 PDF 时只输出当前笔记正文：侧栏、列表、顶栏、格式工具栏、底栏与所有弹层都不再打出来；深色主题下也按黑字白底打印；图片限制在页面宽度内，代码块自动换行不再横向裁切，代码块、引用、表格和列表项尽量不跨页，标题不落在页尾。待办清单以 ☐ ☑ 符号呈现。

## [0.11.0] - 2026-09-20

### 新增

- 每日自动维护。服务端每天执行一次清理任务：Cloudflare 版由 Worker 的定时触发器（每天 03:17 UTC）驱动，独立服务器版在启动 5 秒后先跑一次、之后每 24 小时一次，结果写入日志。任务内容如下三项。
- 孤儿图片回收。从正文里删掉的图片过去会永远留在存储里；现在上传满 7 天且所属笔记正文不再引用的图片会被自动删除。7 天宽限期给撤销和多端同步留出时间，宽限期内一切照旧。
- 回收站自动清理。新增部署级配置 `TRASH_RETENTION_DAYS`，默认关闭；设置后，进入回收站超过该天数的笔记会被自动彻底删除，效果与手动「彻底删除」一致，删除信号照常同步到其他设备。Cloudflare 写在 `wrangler.jsonc` 的 `vars` 里，独立服务器写在 `.env.server` 或 `.env` 里；值必须是正整数天数，非法值会让服务器拒绝启动。
- 墓碑回收定时化。彻底删除后保留 30 天供其他设备同步的删除标记，此前只有手动清空回收站时才会物理清掉，现在由每日任务处理，`note` 表不再随删除无限增长。

## [0.10.0] - 2026-09-20

### 新增

- 搜索多关键词。搜索框里用空格隔开多个词，只列出标题和正文合起来包含全部词的笔记；每个词都在标题里的排最前，其余按更新时间倒序。列表里标题和摘要会把每个词分别高亮，重叠的命中合并成一段；标题和摘要凑不齐全部词时，摘要换成正文里最靠前那处命中附近的片段。分组或星标视图里搜不到东西时，空态给出「在全部笔记中搜索」的按钮，保留搜索词直接切到全部笔记。
- 系统分享到 snotes。安装为 PWA 后，手机分享面板里会出现 snotes：分享一段文字或一个链接会直接新建一条笔记，标题、正文、链接各占一段，正文和链接重复时只保留一份。桌面或主屏图标长按时新增「新建笔记」快捷入口。这两种方式都通过地址参数进入，应用启动后立刻把参数从地址栏擦掉，刷新不会再建一条。目前只支持文字和链接，图片分享和 iOS Safari 暂不支持，前者是后续计划，后者是平台限制。
- 快捷键扩展。除原有的新建（Ctrl/⌘ N）、搜索（Ctrl/⌘ K）外新增：Ctrl/⌘ Shift S 星标、Ctrl/⌘ Shift P 置顶、Ctrl/⌘ Shift D 删除当前笔记（仍会弹确认）、Ctrl/⌘ Alt ↑ ↓ 切换上一条 / 下一条、Ctrl/⌘ Shift F 专注模式、Ctrl/⌘ Shift U 立即同步、Ctrl/⌘ Shift 0 回到全部笔记、Ctrl/⌘ Shift 1–9 切到第 N 个分组。回收站和监控页里对笔记的操作键不生效。完整清单放在编辑器底栏「格式帮助」弹层里，按平台显示 Ctrl 或 ⌘。

## [0.9.0] - 2026-09-20

### 新增

- 深色模式。侧栏底部新增「设置」入口，主题可选跟随系统、浅色、深色，跟随系统时随系统偏好实时切换。深色下六色皮肤板、缩略图、热力图色阶与用量状态色单独校色；已保存的主题在页面首帧前就生效，刷新不会闪白；浏览器地址栏与系统状态栏的颜色随主题变化。
- 正文字号与编辑区宽度。设置里可选小、标准、大三档字号与窄、标准、宽三档编辑区宽度，标题、代码块和表格随字号等比缩放。这三项设置只保存在当前设备的浏览器里，不参与同步。
- 编辑工具栏。正文上方新增一行格式按钮：一级到三级标题、加粗、斜体、删除线、行内代码、无序列表、有序列表、待办清单、引用、代码块、链接、表格。按压即切换，已经在目标格式上再按一次退回上一种；光标落在某种格式里时对应按钮会点亮，标题按钮按级别点亮。按钮不会抢走编辑器的焦点，点完可以直接接着打字，手机上键盘也不会收起。窄屏工具栏横向滚动，不会把页面撑宽。
- 插入链接。工具栏的链接按钮打开一个小弹窗，填地址与可选的显示文字：地址没写协议时自动补 `https://`，站内相对路径原样保留，`javascript:` 这类可执行协议会被拒绝。光标已经在链接上时改为编辑这条链接，只换地址、文字保持不动。
- 插入表格。工具栏一键插入 3×3 表格，行列与对齐由编辑器预设决定，插入后可直接在格子里输入。
- 链接气泡。编辑正文时把光标放进一条链接，链接上方会浮出一排小按钮：打开、编辑、复制、移除。点「编辑」只换地址、链接文字保持不动；点「移除」拆掉链接但保留文字。气泡随编辑器滚动和窗口缩放重新贴位，滚出可视区或编辑器失焦时自动收起。
- 按住 Ctrl（Mac 上是 Cmd）再点链接直接打开：站外链接在新标签页打开，站内相对路径原样跳转。普通点击仍然只是把光标放到链接里，正文里链接较多时避免误触跳转。外链统一带上 `rel="noopener noreferrer"`，新打开的页面拿不到本站的 `window.opener`。
- 图片查看。双击正文里的图片打开全屏灯箱，看清原图细节：滚轮或按钮缩放（1–6 倍）、放大后拖动画面、左右切换同一条笔记里的图片、下载原图。手机上双击常被浏览器抢去做缩放，所以长按图片同样可以打开；手指一滑动就取消，不会和滚动正文打架。灯箱里按 `←` `→` 翻页、`+` `-` 缩放、`0` 回到适应屏幕、`Esc` 关闭。多张图片时右上角显示「第几张 / 共几张」，只有一张时不显示。

### 修复

- 打开应用后点击列表里的笔记，正文不再停在上一条笔记上。格式工具栏会在每次光标或文档变化时读取光标处的格式，而编辑器初始化的早期也会派发事务，那时编辑器视图还没挂上 `state`；按旧写法直接取 `state` 会抛错，这个错又恰好抛在编辑器创建过程中，导致整个编辑器实例建不出来。表象就是：选中的笔记高亮会跟着换，正文却一直停在打开时那一条，格式按钮也全部失灵。现在读不到 `state` 就跳过这一次广播。

## [0.8.0] - 2026-09-19

### 新增

- 导出与导入。侧栏底部新增「导出与导入」入口：一键把全部笔记打包成 zip 下载，包内每个分组一个文件夹、每条笔记一个 `.md`，正文里的图片下载后放进 `images/` 并改成相对路径，另附 `index.json` 记录分组、星标、置顶、颜色与创建时间。导入接受单个或多个 `.md` 以及这份 zip：有 `index.json` 时还原属性与分组，没有时按文件夹名建分组；图片重新上传到当前实例并改写引用；同一份包重复导入不会产生副本。导入的笔记和手写的一样进入同步队列。回收站里的笔记不进备份包。
- 单条笔记导出。编辑器底栏新增「导出与分享」：复制 Markdown、下载 `.md`，手机上还能用系统分享面板把正文发到其他应用。
- 分组管理补全。侧栏分组的「更多」按钮改为菜单：重命名、标记颜色（与笔记同一套六色）、上移、下移、删除。删除前弹窗确认，组内笔记会回到未分组而不会被删除。

### 修复

- 点击正文中的图片时把文本光标放到图片左侧或右侧，不再整体选中图片。此前选中图片后光标不可见，继续打字会把图片替换掉；手机上传的照片在电脑端往往占满整个编辑区，表现为电脑端「无法聚焦、无法打字」。

## [0.7.1] - 2026-09-18

### 修复

- 网页升级到新版后，版本弹窗不再把升级前缓存的旧版本号当作「最新版本」显示。缓存里的最新版比当前运行版本旧时视为过期，立即重新查询；查询失败时也不再回退到这份过时缓存。

## [0.7.0] - 2026-09-18

正式版，包含 0.7.0-beta.1 预览版的全部内容（独立服务器部署、应用内更新提醒、热力图优化）。应用内更新提醒会提示 0.6.3 及更早版本升级到此版本。

### 修复

- 编辑器顶栏新增「插入图片」按钮，通过系统文件选择器从相册或文件里选图上传。手机浏览器普遍不会把剪贴板图片交给网页的粘贴事件，此前手机端没有任何上传图片的途径；桌面端粘贴上传保持不变。

### 已知限制

- 独立服务器默认创建新数据库，不会自动迁移已有 Cloudflare D1/R2 数据；远程使用需配置 HTTPS。
- 直接运行 Node.js 已通过集成、浏览器与重启持久化验证。Docker Compose 配置检查已通过，但镜像构建与容器运行尚未实测。

## [0.7.0-beta.1] - 2026-09-16

预览版（GitHub Prerelease），不替换稳定版 Latest。应用内更新提醒仅查询稳定版；预览版需要手动选择发布标签部署。

### 新增

- 支持独立服务器部署：Node.js 24 + SQLite + 本地图片存储，提供 Docker Compose、systemd 与 HTTPS 反向代理示例。复用原有笔记 API、同步、冲突和回收站逻辑，启动自动执行数据库迁移；版本弹窗按部署方式显示更新步骤。
- 应用启动时检查 GitHub 最新 Release，成功结果缓存 24 小时：有新版本时版本按钮显示蓝点，弹窗列出最新版本号、发布说明链接和自托管更新步骤。检查失败、超时或离线时回退到已有缓存，不影响笔记功能；缓存不可写时仍显示本次检查结果。

### 优化

- 热力图横纵间距统一为 1px，移除均匀分散列宽的布局，格子紧凑相邻。
- 更新热力图改用对比更明显的单色色阶，四档之间亮度差距拉开，无更新的日期使用中性灰并加细描边，与「少量更新」明确区分；有更新的日期至少显示为最浅蓝，不再被淹没在底色里。

### 文档

- README 新增「更新到新版本」与「能否部署到自己的服务器」说明；运维手册补充升级步骤和 `wrangler.jsonc` 冲突处理。
- 完善中英文 README：部署方式对比、预览版安装与标签升级、Node.js / Docker 快速开始、备份恢复入口和令牌轮换。

### 预览版限制

- 独立服务器默认创建新数据库，不会自动迁移已有 Cloudflare D1/R2 数据；远程使用需配置 HTTPS。
- 直接运行 Node.js 已通过集成、浏览器与重启持久化验证。Docker Compose 配置检查已通过，但本次环境未运行 Docker 引擎，镜像构建与容器运行尚未实测。

## [0.6.3] - 2026-09-15

### 优化

- 统一应用标识：侧栏、登录页、浏览器标签和已安装应用改用同一款蓝色圆角底、白色折角便签图标，不再出现纯色占位图标。
- 新增 iOS 主屏幕图标和 Android 自适应图标，系统按各自形状裁切时图案保持一致；便签图案保留在中心安全区内。
- 图标改为由唯一的 SVG 源文件生成，浏览器优先使用矢量图标，高分屏下更清晰；图标资源进入离线预缓存。

## [0.6.2] - 2026-09-14

### 优化

- 参考滴答清单整理窄图标导航、紧凑笔记目录和浅灰选中态，统一编辑工具、分组操作和弹窗样式。
- 回收站入口移至“星标笔记”下方，与普通笔记共用相同的侧栏、列表宽度和预览位置。
- 统计改为大弹窗，图表在弹窗内滚动，保留原页面布局、编辑光标和撤销记录；支持关闭按钮、遮罩、Esc 和系统返回。
- 返回笔记时恢复之前的分组、搜索条件、选中笔记和滚动位置。
- 编辑器、统计内容和图表样式继续按需加载，弹窗使用 CSS 动画，没有新增运行时依赖。
- 更新中英文 README，补充界面操作、快捷键、部署和备份说明。

### 修复

- 从统计榜单打开笔记时正确加载目标列表并解除旧筛选，返回后还原原来的笔记位置。
- 修复手机关闭统计或切换侧栏视图后，焦点落到隐藏入口的问题。
- 导航离开前记录当前选中项和滚动位置，浏览器返回后不再丢失阅读进度。

## [0.6.1] - 2026-09-14

### 优化

- 精简统计页顶栏、品牌标语、分组提示、回收站及版本信息中的重复说明，回收站仅保留一处空状态。
- 统一灰白底色、低饱和蓝色强调与回收站图标，简化笔记目录的选中高亮。
- 字数统计弹窗的关闭按钮在桌面和手机上居中，统计页在窄屏下保留侧栏入口。
- 清理重复和闲置样式，继续按需加载编辑器与统计页；没有新增运行时依赖，入口 JavaScript 与 CSS 的 gzip 合计从约 87.0 KB 降至 85.9 KB。

### 修复

- 修复粘贴富文本有序列表时，空的 `start` 属性被误转为 0 的问题；默认从 1 开始，保留明确指定的起始编号。
- 修复非 1 起始的嵌套列表保存为 Markdown 后失去层级的问题，刷新后继续保留编号与格式。

## [0.6.0] - 2026-09-13

### 新增
- 编辑区支持专注模式，按 Esc 返回常规布局。
- Markdown 任务清单支持直接勾选，并支持保存、撤销和重做。

### 优化
- 更新三栏布局、配色、正文排版和空状态，统一图标与操作按钮。
- 搜索移到列表顶部，手机上直接可见，支持 Ctrl / Cmd + K、结果计数和键盘选择。
- 字数、文档信息与格式提示集中到编辑区底部，让常用操作更清晰。
- 改善手机侧栏、返回和滑动操作，以及弹窗的焦点管理和键盘操作。
- 编辑器、统计页与统计样式按需加载，首屏入口脚本 gzip 从约 220 KB 降至 81 KB，保留 PWA 离线能力。

### 修复
- 搜索或统计视图中新建便签后立即显示并聚焦编辑器，可以直接输入。
- 修复中文输入法选词时误触搜索跳转或分组提交的问题。

## [0.5.0] - 2026-09-09

### 新增
- 左侧导航栏底部显示当前网页版本号，点击可查看版本信息，版本号来自 `package.json`。

### 修复
- 底部云朵图标支持点击手动同步，同步期间显示加载状态并避免重复点击，异常时显示错误提示。

## [0.4.2] - 2026-09-09

### 修复
- 当前笔记在同步拉到新正文后立即刷新编辑器内容，不再停留在旧版本；编辑期间收到的远端正文不会再覆盖正在输入的内容。
- 冲突副本保存服务端被覆盖的旧正文，不再存成本地胜出的内容；两设备同时修改同一版本正文时，至少一台会收到冲突副本而不是静默丢字。
- 创建失败（如网络抖动）时笔记正文会在恢复后自动补发，不再永久丢失。
- 分组视图里新建的笔记就地归入当前分组；移动端新建后按返回键先回到目录而不是退出应用。
- 离线删除、恢复交替操作后各设备状态保持一致。
- 远端更新正文后，列表里的标题与摘要立即与正文同步。
- 只有分组变化时侧栏也会刷新；彻底删除后的笔记不再能被恢复出空白内容。

## [0.4.1] - 2026-09-08

### 修复
- 访问令牌过期后回到登录页时明确提示「令牌已失效」，不再与首次使用混淆。
- 令牌失效瞬间停止无意义的同步重试，避免令牌已清空后仍在空转几百次请求、拖慢界面。
- 网络请求最多等待 15 秒，此前请求挂起时同步引擎会被永久占住。
- 服务端已删除的笔记不再让同步任务永久标记失败，界面上的「改动未推送」提示可以正常消失。
- 监控页用量数据缓存 60 秒，快速来回切视图不再重复消耗 Cloudflare Analytics 额度。

## [0.4.0] - 2026-08-29

### 新增
- 打开次数改为按设备累计并同步聚合，统计页显示全设备总次数与最近打开记录。
- 统计页改为响应式 Bento 网格，新增连续写作、24 小时写作时段、30 天创建/更新趋势和长度分布。

### 修复
- 笔记列表与回收站首次加载、切换视图时先显示骨架屏，不再短暂显示错误的空状态或旧列表。
- 统计页使用全部可用宽度展示，更新热力图不再出现横向滚动条，趋势图直接展示数据汇总与空态。
- 搜索命中正文时，列表会展示命中位置附近的正文片段并高亮关键词。

## [0.3.1] - 2026-08-26

### 变更
- 暂时移除侧栏中的用量监控入口，监控页面及相关能力保留。

## [0.3.0] - 2026-08-26

### 新增
- 详情顶部新增文档信息入口，可弹窗查看创建时间、更新时间、最近打开、打开次数和内容版本。
- 详情顶部新增字数统计入口，可实时查看字数、非空行和可见字符。
- 新增统计页，提供写作概览、近一年更新热力图、近 7 天创建/更新趋势、分组分布和最常打开笔记。
- 打开次数与最近打开时间仅保存在本地，不进入同步流程，也不会改变笔记列表排序。

## [0.2.2] - 2026-08-26

### 修复
- 粘贴或回车产生的空行不再被写成 `<br />` 字面 HTML。此前 Milkdown 把空段落序列化成 `<br />` 写进 markdown，加载时被 XSS 防线 `escapeRawHtml` 转成 `&lt;br />` 字面文本，既破坏空行视觉又留下乱码。现在空段落改用零宽空格承载，remark 往返不折叠、防线不触碰、视觉不可见。
- 旧笔记里已有的 `<br />` 空行打开时自动迁移成零宽空格，恢复空行视觉；代码块与行内代码内的 `<br />` 不动，仍按原始 HTML 转义展示。

## [0.2.1] - 2026-08-25

### 修复
- 粘贴图片后占位图换成真实地址时不再闪屏：先预载真实图，下载完再替换 <img> 节点。
- 修复含 blob: 占位的正文在上传未完成时被持久化、并可能被同步推到服务端，导致其它设备拉到死链且不再收敛的问题。

## [0.2.0] - 2026-08-25

### 新增
- 移动端安装到桌面后，打开应用默认展示目录页（列表），不再直接进入详情页，与便签类应用一致。

### 修复
- 移动端在目录页按系统返回键可直接退出回桌面；此前因导航栈底压了哨兵条目并被 `forward()` 挡回，只能大退退出。

## [0.1.0] - 2026-08-25

首个有明确应用版本号的基线版本，包含当前已部署的个人 Markdown 便签、离线优先、增量同步、图片存储和冲突副本能力。
