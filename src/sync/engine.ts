import {pull} from './pull';import {push} from './push';import {getToken} from '../api/token';let timer:number|undefined;export function startSync(){const run=()=>{if(!getToken())return;void push().then(pull).catch(()=>{} )};run();window.addEventListener('snotes:token',run);window.addEventListener('online',run);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')run()});timer=window.setInterval(run,30000);return()=>{window.removeEventListener('snotes:token',run);window.removeEventListener('online',run);if(timer)clearInterval(timer)}}
