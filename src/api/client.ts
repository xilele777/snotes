import {getToken} from './token'; export async function apiFetch<T=any>(path:string,init:RequestInit={}){const h=new Headers(init.headers);const t=getToken();if(t)h.set('Authorization',`Bearer ${t}`);if(init.body&&!(init.body instanceof FormData))h.set('Content-Type','application/json');const r=await fetch(`/api${path}`,{...init,headers:h});if(r.status===401)window.dispatchEvent(new Event('snotes:unauthorized'));if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.status===204?undefined:r.json() as Promise<T>}
export async function apiUpload(file:File,noteId:string){const f=new FormData();f.append('file',file);f.append('note_id',noteId);return apiFetch<{url:string}>('/images/upload',{method:'POST',body:f})}
